---
phase: 260512-u20-restructure-readme-md-for-v1-milestone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - quick-readme-v1
must_haves:
  truths:
    - "A first-time visitor sees the 'load a published model' value proposition before they see any training instructions"
    - "The 'load a published model' snippet is honest about v1 publication status (Coming in v1, not 'works today')"
    - "No reference to non-existent scripts (pz_push_model, pz_prepare_train) or files (train_node.py) remains in the README"
    - "The supported-datasets list matches the real importers in configs/dataset_import/"
    - "The empty Performance section, the Discord badge, and the placeholder Citation block are removed"
    - "The Mermaid diagram still renders and reflects only files/scripts that exist"
  artifacts:
    - path: "README.md"
      provides: "Restructured README with consumer-first narrative"
      contains: "huggingface.co/project-oceania"
  key_links:
    - from: "README.md (header)"
      to: "huggingface.co/project-oceania"
      via: "consumer-path section + AutoModelForImageClassification snippet"
      pattern: "from_pretrained.*project-oceania"
    - from: "README.md (Mermaid diagram)"
      to: "real files in planktonzilla/"
      via: "node labels point only to files that exist on disk"
      pattern: "(?!.*train_node\\.py)(?!.*pz_prepare_train)(?!.*pz_push_model)"
---

<objective>
Restructure README.md so it leads with the v1 milestone's headline artifact — the pre-trained plankton-classification models on Hugging Face Hub — instead of leading with framework training instructions. Demote training to a maintainer section, remove three stale references to non-existent scripts/files, expand the supported-datasets list to match reality, and cut three empty/placeholder sections. Single-file, doc-only change.

Purpose: The current README is written for someone who wants to TRAIN a model. The v1 audience (external researchers, marine biologists, students, paper/grant reviewers) wants to LOAD a published model. This rewrite aligns the README's opening promise with the milestone's actual deliverable.

Output: Rewritten `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@README.md
@pyproject.toml
@planktonzilla/clip_model.py
@.planning/STATE.md

<interfaces>
<!-- Ground truth the executor must use. Do not invent values; copy from these. -->

**Real console scripts (from `pyproject.toml [project.scripts]`):**
- `pz_import_dataset` → `planktonzilla.dataset_import.import_dataset:main` (module EXISTS)
- `pz_train` → `planktonzilla.train:main` (module EXISTS)
- `pz_prepare_train` → `planktonzilla.prepare_train:main` (module DOES NOT EXIST — declared but unimplemented; do NOT mention in README)
- `pz_push_model` → `planktonzilla.push_model:main` (module DOES NOT EXIST — declared but unimplemented; do NOT mention in README)

**Real dataset importers (from `configs/dataset_import/*.yaml`, excluding `default.yaml`):**
- `flowcamnet`
- `global_uvp5net`
- `isiisnet`
- `jedi_oceans_cpics`
- `lensless`
- `medplanktonset`
- `planktonset1`
- `planktoscope`
- `syke_ifcb_2022`
- `sykezooscan2024`
- `uvp6net`
- `whoi-plankton`
- `zoocamnet`
- `zoolake`
- `zooscannet`

(15 importers. The README currently lists 7 — incomplete.)

**v1 publication status (from `.planning/STATE.md`):**
- Project is in Phase 1 (Foundation). No models are published to `huggingface.co/project-oceania` yet.
- REL-06 (CLIP model packaging, the headline of v1) is in Phase 3.
- THEREFORE: the "load a published model" snippet MUST be presented as the v1 target API, NOT as a working example. Frame it as "Coming in v1" or under a section header that makes the not-yet-published status unambiguous.

**v1 target loading API (from `CLAUDE.md` + research/STACK.md decisions in STATE.md):**
- Decided pattern: `PreTrainedModel` + `register_for_auto_class` + `trust_remote_code=True`, so a single universal snippet works for all 7 v1 models.
- Snippet shape (this is the target — write it as such, do not promise it works today):

```python
from transformers import AutoModelForImageClassification, AutoImageProcessor
from PIL import Image

model_id = "project-oceania/<model-name>"  # see https://huggingface.co/project-oceania
processor = AutoImageProcessor.from_pretrained(model_id, trust_remote_code=True)
model = AutoModelForImageClassification.from_pretrained(model_id, trust_remote_code=True)

image = Image.open("plankton.jpg").convert("RGB")
inputs = processor(images=image, return_tensors="pt")
outputs = model(**inputs)
predicted_idx = outputs.logits.argmax(-1).item()
print(model.config.id2label[predicted_idx])
```

**Mermaid diagram — required edits:**
- DELETE node: `TRAIN_NODE["planktonzilla/train_node.py"]` (file does not exist)
- DELETE edge: `ENTRY -->|spawns| TRAIN_NODE`
- EDIT node `CLI`: change `"CLI: console scripts (pz_train, pz_prepare_train, pz_import_dataset)"` to `"CLI: console scripts (pz_train, pz_import_dataset)"` (drop `pz_prepare_train`)
- Keep ALL other nodes and edges from the last commit unchanged.

**Sections to CUT entirely:**
- The Discord badge line in the header block (line 17)
- The "📊 Performance" section (currently lines 204–206)
- The "🏛️ Citation" section (currently lines 221–233) — placeholder bibtex with stale 2024 year, no DOI

**Sections to PRESERVE (but possibly relocate):**
- The header div, logo placeholder, project title, OcéanIA attribution, remaining badges
- The "Features" bullet list — fine as-is, can stay near top
- Installation instructions
- "Basic Usage" → demoted under a "For maintainers / Train your own" heading
- "Project Structure" tree
- "Advanced Usage" → demoted under maintainer section
- "Supported Datasets" → expanded to 15 entries
- "Loss Functions for Imbalanced Learning"
- "Experiment Tracking"
- "🧪 Development" block
- "🤝 Contributing"
- "📄 License"
- "📞 Support"
- The Mermaid diagram (with the edits above)
- The closing "Built with ❤️ by Inria Chile" footer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with consumer-first narrative</name>
  <files>README.md</files>
  <action>
Rewrite README.md as a single Edit/Write operation. The new structure, in order from top:

1. **Header block** — keep the emoji + title (`# 🪸 🦠 🪼 🦐 🦖 🐙 🫧 🌊\n\`planktonzilla\``), keep the "Deep learning framework, datasets, and models for plankton identification." tagline, keep "Part of [Inria Challenge OcéanIA]". REMOVE the Discord badge line. Keep the Python / PyTorch / transformers / datasets / huggingface_hub / Hydra / Poetry badges.

2. **One-sentence lead** — replace the current paragraph with a single sentence that frames the headline artifact: "Curated, citable pre-trained plankton-classification models on Hugging Face Hub — load one and run inference on plankton images in under ten lines of Python." Then the two existing bullet links (OcéanIA website, HF org).

3. **`## Load a pre-trained model`** — NEW section, immediately after the lead, BEFORE installation. This is the section that must be in the first screenful. Open with a one-line note that the models are landing in v1 (see status link below) and that the snippet below is the target API every published model will conform to. Then the Python snippet from `<interfaces>` above, verbatim, in a python fenced block. After the snippet, add three short lines: (a) "Browse published models: https://huggingface.co/project-oceania", (b) "No clone of this repo is required — `pip install transformers pillow` is the consumer dependency surface", (c) "v1 status: pre-trained models are being prepared for release; see [project status](https://oceania.inria.cl/) and the HF org page above for current availability." Do NOT claim the snippet works today. Do NOT invent a specific model name; use `<model-name>` as a placeholder in the snippet as shown in `<interfaces>`.

4. **`## Features`** — keep the existing 7-bullet list as-is.

5. **`## Supported datasets`** — list ALL 15 importers from `<interfaces>`. Format each as a single bullet with the importer name in code (the value users pass to `dataset_import=`) and a short human-readable label. Example: ``- `isiisnet` — In-Situ Ichthyoplankton Imaging System Network``. Use the human-readable names from the existing README where available; for new entries (global_uvp5net, jedi_oceans_cpics, medplanktonset, planktonset1, planktoscope, syke_ifcb_2022, sykezooscan2024, zoocamnet, zooscannet), write short factual labels (one short noun phrase each — do not invent provenance details you cannot verify from the importer name itself).

6. **`## License`** — short paragraph, link to LICENSE file, mention MIT. (Promote from bottom — license matters to the reviewer audience.)

7. **`---`** — horizontal rule separator.

8. **`## For maintainers / Train your own model`** — H2 header introducing the rest of the document as maintainer-facing. One short intro sentence: "Everything below is for contributors and researchers who want to train new models or extend the framework. Consumers loading a published model do not need any of it."

9. **`### Prerequisites`**, **`### Installation`** — move the existing content under here (clone, `poetry install`, `poetry install --with dev`, `poetry shell`).

10. **`### Import a dataset`** — keep the existing examples (`pz_import_dataset dataset_import=isiisnet`, etc.).

11. **`### Train a model`** — keep the four existing `pz_train` examples.

12. **REMOVE the existing `#### 3. Push Model to Hub` subsection entirely.** Do NOT replace it with anything. The publishing path for v1 models is internal and will be documented elsewhere; the README must not point users at `pz_push_model` because the module does not exist.

13. **`### Project structure`** — keep the existing tree.

14. **`### Configuration system`** — keep the Hydra override examples.

15. **`### Loss functions for imbalanced learning`** — keep the 6-bullet list.

16. **`### Experiment tracking`** — keep the two examples.

17. **`### Development`** — keep "Running Tests", "Code Quality", "Adding New Datasets", "Custom Loss Functions".

18. **REMOVE the entire `## 📊 Performance` section.** Do not replace.

19. **`## Contributing`** — keep as-is.

20. **REMOVE the entire `## 🏛️ Citation` section.** Do not replace. (Will be re-added when there is a real DOI to cite — outside this milestone.)

21. **`## Support`** — keep as-is (homepage, issues, email).

22. **Mermaid diagram** — preserve from the last commit (903cb09) with these edits ONLY:
    - In the `CLI` node label, remove `pz_prepare_train`. Final label: `"CLI: console scripts (pz_train, pz_import_dataset)"`.
    - Delete the line `TRAIN_NODE["planktonzilla/train_node.py"]`.
    - Delete the edge line `ENTRY -->|spawns| TRAIN_NODE`.
    - Leave every other node and edge untouched (CFG, ENTRY, DATA_IMPORT, DATA, MODEL, LOSS, TRAIN_LOOP, TRACK, OUTPUTS, SCRIPTS, TESTS, ConfigFlow subgraph, all other edges).
    - The diagram should still parse as Mermaid `flowchart TD`.

23. **Footer** — keep the existing centered "Built with ❤️ by Inria Chile" div.

Style rules:
- Tone: factual, no marketing superlatives. The audience is paper reviewers — they smell hype.
- Do NOT add badges, sections, emojis, or links not enumerated above. Resist the urge to "polish".
- Do NOT invent dataset descriptions or DOIs. If you don't know a dataset's full name from the existing README content, use the importer key with a short factual label only.
- Section headers in the consumer half use sentence case (`## Load a pre-trained model`), matching the established README voice. Existing emoji headers in the maintainer half (`## 🧪 Development`, etc.) can stay if they already use emoji, but the new top sections should NOT introduce new emoji.
- The "Load a pre-trained model" section must appear before the Features bullet list — Features is supporting context, the load snippet is the lead.

Constraints (locked, do not revisit):
- Single-file change. Do not touch anything outside `README.md`.
- Do not add a new "Quickstart for consumers" subsection in addition to "Load a pre-trained model" — that section IS the consumer quickstart. One section, not two.
- Do not modify `pyproject.toml` to "fix" the missing-module declarations. That's out of scope for this README pass.
  </action>
  <verify>
    <automated>test "$(grep -c 'pz_push_model' README.md)" -eq 0 && test "$(grep -c 'pz_prepare_train' README.md)" -eq 0 && test "$(grep -c 'train_node\.py' README.md)" -eq 0 && test "$(grep -c 'TRAIN_NODE' README.md)" -eq 0 && test "$(grep -c '📊 Performance' README.md)" -eq 0 && test "$(grep -c '🏛️ Citation' README.md)" -eq 0 && test "$(grep -c 'shields.io/discord' README.md)" -eq 0 && grep -q 'project-oceania' README.md && grep -q 'AutoModelForImageClassification' README.md && grep -q 'trust_remote_code' README.md && for d in flowcamnet global_uvp5net isiisnet jedi_oceans_cpics lensless medplanktonset planktonset1 planktoscope syke_ifcb_2022 sykezooscan2024 uvp6net whoi-plankton zoocamnet zoolake zooscannet; do grep -q "\`$d\`" README.md || { echo "MISSING dataset: $d"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>
README.md has been rewritten so that:
- The "Load a pre-trained model" section with the `AutoModelForImageClassification` snippet appears in the first screenful, before training instructions.
- The snippet is framed as the v1 target API, not as a working example (no false claim that models are already published).
- All three stale references are gone: `pz_push_model`, `pz_prepare_train`, `train_node.py` / `TRAIN_NODE`.
- The "Supported datasets" section lists all 15 importer keys from `configs/dataset_import/`.
- The Discord badge, the "📊 Performance" section, and the "🏛️ Citation" section are removed.
- The Mermaid diagram renders and contains no reference to the deleted node or to `pz_prepare_train`.
- All other existing sections (Features, Installation, Train a model, Project structure, Loss functions, Experiment tracking, Development, Contributing, Support, footer) are preserved, with training-related content placed under a clearly demarcated "For maintainers" H2.
- Automated grep verification passes (the one-liner in `<verify>` exits 0 and prints `OK`).
  </done>
</task>

</tasks>

<verification>
The automated grep in Task 1's `<verify>` block covers every must-have truth:
- Stale references gone: 4 negative greps (`pz_push_model`, `pz_prepare_train`, `train_node\.py`, `TRAIN_NODE`).
- Cut sections gone: 3 negative greps (`📊 Performance`, `🏛️ Citation`, Discord badge URL).
- Consumer-path present: 3 positive greps (`project-oceania`, `AutoModelForImageClassification`, `trust_remote_code`).
- Dataset list complete: loop over 15 importer keys, fails fast if any missing.

In addition, a human eyeball check should confirm:
- The "Load a pre-trained model" section appears before the Features list (consumer-first ordering).
- The snippet does not claim models are already on the Hub today — it is framed as the v1 target API.
- The Mermaid diagram still renders (paste into a Mermaid live editor or any Markdown viewer that supports Mermaid).
</verification>

<success_criteria>
- `README.md` is the only modified file (`git status` shows exactly that).
- The automated grep one-liner in Task 1's `<verify>` exits 0 and prints `OK`.
- A reader scrolling from the top sees the v1 promise (load a published model) before any training command.
- The snippet's honesty bar is met: no "this works today" claim against an unpublished Hub org.
- The Mermaid diagram parses and references only files that exist on disk.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-u20-restructure-readme-md-for-v1-milestone/260512-u20-SUMMARY.md`.
</output>
